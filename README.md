# openpi user client

This directory contains the user-side OpenPI project setup and runtime code for the ManiSkill workspace.

It is packaged with Hatchling so local `uv sync` and editable installs can resolve and build it correctly.
